--------------------------------------------------------
-- Dashboard
--------------------------------------------------------

SELECT * FROM vw_dashboard_summary;

--------------------------------------------------------
-- Leaderboard
--------------------------------------------------------

SELECT * FROM vw_leaderboard;

--------------------------------------------------------
-- ESG Summary
--------------------------------------------------------

SELECT * FROM vw_esg_summary;

--------------------------------------------------------
-- Department Ranking
--------------------------------------------------------

SELECT

name,

total_score

FROM vw_esg_summary

ORDER BY total_score DESC;

--------------------------------------------------------
-- Notifications
--------------------------------------------------------

SELECT *

FROM vw_recent_activity

LIMIT 10;

--------------------------------------------------------
-- Total Carbon Emissions
--------------------------------------------------------

SELECT

SUM(calculated_co2e)

FROM carbon_transactions;

--------------------------------------------------------
-- Open Compliance Issues
--------------------------------------------------------

SELECT *

FROM compliance_issues

WHERE status='OPEN';

--------------------------------------------------------
-- Active Challenges
--------------------------------------------------------

SELECT *

FROM challenges

WHERE status='ACTIVE';

--------------------------------------------------------
-- CSR Activities
--------------------------------------------------------

SELECT *

FROM csr_activities

WHERE status='OPEN';

--------------------------------------------------------
-- Rewards

SELECT *

FROM rewards

WHERE stock>0;